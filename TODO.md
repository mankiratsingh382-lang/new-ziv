# TODO

## About page photo responsiveness
- [x] Update `about.html` local CSS to prevent `.about-photo-card` / `.about-photo-wrap` from collapsing on desktop.
- [x] Set a stable desktop height using `clamp()` so the absolutely positioned image shows fully.
- [ ] Verify in browser that the desktop photo is not cut to half.


