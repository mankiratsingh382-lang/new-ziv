# TODO

- [x] Remove inline `<style>` from `zivarr.html` and link to `style.css`.
- [x] Move inline `<script>` from `zivarr.html` to `script.js`.
- [x] Update `zivarr.html` to load `script.js` via `<script src="script.js" defer></script>`.
- [ ] Ensure `style.css` contains all CSS formerly inside `zivarr.html`.
- [ ] Open `zivarr.html` in browser and verify preloader, custom cursor, scroll progress, reveal-on-scroll, filters, and hero parallax.


